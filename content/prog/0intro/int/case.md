+++
title = "case"
subsections = ["intermediate"]
+++

Strings contain a lot of helper functions. Two of them are `upper` and
`lower`:

	>>> foo = "HeLlO tHERe"
	>>> foo.lower()
	'hello there'
	>>> foo.upper()
	'HELLO THERE'
